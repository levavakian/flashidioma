{
  "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "install": "npm ci && npx playwright install chromium",
  "start": "echo \"FlashIdioma cloud environment ready.\"",
  "terminals": [
    {
      "name": "app",
      "command": "npm run dev -- --host 0.0.0.0 --port 4174",
      "ports": [4174]
    }
  ]
}
